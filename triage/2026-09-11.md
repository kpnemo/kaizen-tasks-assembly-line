# Triage 2026-09-11

Rubric version 1. 5 issues scored at 2026-09-11T19:37:40Z.

| Rank | Issue | Title                                                   | Readiness | Clarity | Complexity | Risk | Arch |
| ---- | ----- | -------------------------------------------------------- | --------- | ------- | ---------- | ---- | ---- |
| 1    | #2    | Add a "Mark all steps done" button on the task detail    | 17        | 5       | 2          | 3    | no   |
| 2    | #5    | Regenerate suggestions with a hint                        | 16        | 5       | 3          | 3    | no   |
| 3    | #29   | Simplify header into theme toggle and user dropdown menu  | 16        | 4       | 2          | 2    | no   |
| 4    | #3    | Make the AI smarter                                       | 11        | 2       | 2          | 3    | no   |
| 5    | #4    | Share a task with a teammate                              | 12        | 5       | 5          | 5    | yes  |

## #2 Add a "Mark all steps done" button on the task detail

- Clarity 5: Testable acceptance criteria (button label, which steps count, disabled state, progress label format) plus a named out-of-scope list and an edge case (already-done state disables the button).
- Complexity 2: Confined to the web repo's task-detail feature folder, reusing the existing `PATCH /api/v1/tasks/:id` endpoint; no schema or contract change.
- Risk 3: Touches stored data — bulk-updates step status on multiple rows via the existing update endpoint.
- Architecture change: no
- Questions: none

## #5 Regenerate suggestions with a hint

- Clarity 5: Detailed, checkable acceptance criteria (field length, validation error shape, contract updates) plus explicit out-of-scope and an edge case (empty hint behaves like today).
- Complexity 3: Spans both repos — a new optional body field on `POST /api/v1/tasks/:id/breakdown`, a contract and generated-types update, and a web-side hint field.
- Risk 3: Touches the AI prompt's inputs — the hint is passed to the breakdown model as part of the task input.
- Architecture change: no — the system prompt and output schema are explicitly unchanged; only an input field is added.
- Questions: none

## #29 Simplify header into theme toggle and user dropdown menu

- Clarity 4: Testable acceptance criteria (icon matches mode, click-cycle order, persistence across reload) and a stated scope, but no edge case is named.
- Complexity 2: Web-only header/menu rework, a few components, no schema or contract change.
- Risk 2: An isolated UI control (theme toggle, dropdown reorganizing existing links) that does not affect other features or stored data.
- Architecture change: no
- Questions: none

## #3 Make the AI smarter

- Clarity 2: The problem and the wish are stated, but "the suggestions are better" gives no observable behavior a tester could check.
- Complexity 2: Implies a change confined to the agent module (e.g. `src/agent/prompts/breakdown.system.md`), a few files, no schema change.
- Risk 3: Touches the AI prompt or its inputs.
- Architecture change: no — clarity is below 3, so the architecture test is not applied; a clearer request could reveal a prompt-schema change later.
- Questions:
  1. Who is the user and what kind of work are the current suggestions missing for them?
  2. What would a suggested step look like when it works, versus what it looks like today?
  3. What would make you say the suggestions are "smarter" — what should a tester check?

## #4 Share a task with a teammate

- Clarity 5: Detailed, checkable acceptance criteria (badge text, permission split for tags, error message, unshare behavior) plus out-of-scope and an edge case (unknown email).
- Complexity 5: Restructures how tasks relate to users — introduces multi-user ownership and near-real-time visibility across accounts, not a single new subsystem.
- Risk 5: Changes the multi-user boundary — a task becomes visible and editable by more than its owner.
- Architecture change: yes — multi-user data sharing.
- Questions: none
