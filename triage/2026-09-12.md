# Triage 2026-09-12

Rubric version 1. 5 issues scored at 2026-09-12T12:10:51Z.

| Rank | Issue | Title                                                    | Readiness | Clarity | Complexity | Risk | Arch |
| ---- | ----- | -------------------------------------------------------- | --------- | ------- | ---------- | ---- | ---- |
| 1    | #2    | Add a "Mark all steps done" button on the task detail    | 17        | 5       | 2          | 3    | no   |
| 2    | #5    | Regenerate suggestions with a hint                       | 16        | 5       | 3          | 3    | no   |
| 3    | #3    | Make the AI smarter                                      | 11        | 2       | 2          | 3    | no   |
| 4    | #34   | Size the automatic breakdown to the task, up to fifty steps | 16     | 5       | 3          | 3    | yes  |
| 5    | #4    | Share a task with a teammate                             | 12        | 5       | 5          | 5    | yes  |

## #2 Add a "Mark all steps done" button on the task detail

- Clarity 5: Five yes-or-no criteria (button shown, statuses set through the existing PATCH, label reads N/N, suggested and dismissed steps untouched, button disabled when every counted step is done), with out of scope and the all-done edge case stated.
- Complexity 2: Web only, inside `src/features/tasks/` (task detail bulk bar, a hook calling the existing update endpoint, component tests); no API or schema change.
- Risk 3: Writes step status to stored data in bulk through `PATCH /api/v1/tasks/:id`; no prompt or authentication involvement.
- Architecture change: no
- Questions: none

## #5 Regenerate suggestions with a hint

- Clarity 5: Criteria name the field limit (300 characters), the error code and path, the model input field, the replace-and-keep behaviour and the contract artifacts; out of scope and the empty-hint edge case are stated.
- Complexity 3: Both repos: API `src/routes/` breakdown route, `src/schemas/` body schema, `src/agent/` task input and `openapi.json`; web `src/features/tasks/` regenerate control and `src/api/` regenerated types.
- Risk 3: Adds a user-supplied field to the AI prompt's inputs; the system prompt and output schema stay as they are.
- Architecture change: no
- Questions: none

## #3 Make the AI smarter

- Clarity 2: "The suggestions are better" is a goal with no observable behaviour and no yes-or-no check a tester could run.
- Complexity 2: The only area implied is the breakdown prompt `src/agent/prompts/breakdown.system.md` in one repo, but nothing concrete is specified.
- Risk 3: Any change would touch the AI prompt.
- Architecture change: no
- Questions: 1. Who is the user and what kind of work are the tasks for when the suggested steps miss the mark? 2. For one concrete task you have in mind, what would the suggested steps look like when it works? 3. What would make you say it is done?

## #34 Size the automatic breakdown to the task, up to fifty steps

- Clarity 5: Six yes-or-no criteria covering small, large and mixed tasks, the fifty-step ceiling with one retry, the over-fifty fallback and unchanged accept, edit and dismiss; out of scope and the zero-step and over-fifty edge cases are named.
- Complexity 3: API `src/agent/prompts/breakdown.system.md` (size judgement, variable count, ceiling), the `src/agent/` breakdown module and `src/jobs/` worker (count check, one re-ask), and the web task detail in `src/features/tasks/` for the skipped reason; no schema change.
- Risk 3: Changes the AI prompt's instructions and how the breakdown flow handles the model's output.
- Architecture change: yes
- Questions: none

## #4 Share a task with a teammate

- Clarity 5: Six checkable criteria (share by email, badge and access, mutual edits visible within five seconds, unknown-email error, unshare, tags read-only) with out of scope and the unknown-email edge case.
- Complexity 5: A shares table in `src/db/schema.ts` and `drizzle/`, ownership checks in `src/services/` and `src/repositories/`, new routes and `openapi.json`, and the web task list and detail in `src/features/tasks/`; it changes how the existing task list, detail and step endpoints relate across users.
- Risk 5: Changes multi-user boundaries: a second user gains write access to another user's task and steps.
- Architecture change: yes
- Questions: none
