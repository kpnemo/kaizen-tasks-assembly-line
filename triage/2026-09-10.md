# Triage 2026-09-10

Rubric version 1. 5 issues scored at 2026-09-10T15:16:27Z.

| Rank | Issue | Title | Readiness | Clarity | Complexity | Risk | Arch |
|---|---|---|---|---|---|---|---|
| 1 | #2 | Add a "Mark all steps done" button on the task detail | 17 | 5 | 2 | 3 | no |
| 2 | #19 | Change app accent color from blue to dark brown/reddish | 17 | 4 | 2 | 1 | no |
| 3 | #5 | Regenerate suggestions with a hint | 16 | 5 | 3 | 3 | no |
| 4 | #3 | Make the AI smarter | 11 | 2 | 2 | 3 | no |
| 5 | #4 | Share a task with a teammate | 12 | 5 | 5 | 5 | yes |

## #2 Add a "Mark all steps done" button on the task detail

- Clarity 5: Every criterion is a yes/no check (the button appears when at least one accepted or user-created step is not done, clicking it sets each of those steps to done through the existing `PATCH /api/v1/tasks/:id`, the progress label reads `N/N`, suggested and dismissed steps keep their state and status, the button is disabled when all counted steps are already done, plus named component tests), and the request names three out-of-scope items and the all-done disabled edge case.
- Complexity 2: Confined to one web feature folder, `frontend/src/features/tasks/` (the bulk bar component, its hook and component tests); the request states no API change is needed, so one repo, a few files, no schema change.
- Risk 3: It bulk-writes the stored status of every accepted or user-created step, with undo explicitly out of scope, so it touches stored data even though each write goes through the existing per-step update endpoint.
- Architecture change: no
- Questions: none

## #19 Change app accent color from blue to dark brown/reddish

- Clarity 4: Three criteria a tester can answer yes or no (primary button background is dark brown/reddish rather than blue, active and focused controls such as checkboxes and toggles show the new color, links display in the new accent) with a stated app-wide scope and one out-of-scope item, but no edge case is named (for example the accent in dark theme, hover and disabled states, or focus-ring contrast), so it stops short of 5.
- Complexity 2: Web only: the accent token in the global stylesheet, the shadcn primitives in `frontend/src/components/ui/` (button, checkbox, switch) that consume it, and any hard-coded blue link classes under `frontend/src/features/`; one repo, a few files, no schema change.
- Risk 1: Cosmetic: a color change to the accent tokens and the classes that reference them; no behavior, stored data, or prompt is affected.
- Architecture change: no
- Self-score check: the request body carries a self-score of clarity 4, complexity 2, risk 1, architecture change no, readiness 17; this triage agrees on every scale and on the total.
- Questions: none

## #5 Regenerate suggestions with a hint

- Clarity 5: Each criterion is individually checkable (an optional single-line hint field capped at 300 characters, an empty hint behaving exactly as today, `POST /api/v1/tasks/:id/breakdown` accepting an optional `{ "hint": string }` body, an over-length hint returning `VALIDATION_ERROR` at `body.hint`, an API test that the fake model receives the hint, the thinking state and replaced suggestions with accepted steps kept, and updated `openapi.json`, generated types and `CHANGELOG.md`), and it names three out-of-scope items plus the over-length edge case.
- Complexity 3: Both repos change: the API's breakdown route and `src/schemas/` gain the optional body, `src/agent/` passes the hint into the model input and `openapi.json` is regenerated, while `frontend/src/features/tasks/` gains the hint field and `frontend/src/api/` regenerates its types.
- Risk 3: The hint is passed to the breakdown model as part of the task input, so it touches the AI prompt's inputs, even though the system prompt and the output shape are explicitly held unchanged.
- Architecture change: no
- Questions: none

## #3 Make the AI smarter

- Clarity 2: The single acceptance criterion, "The suggestions are better", is an adjective rather than a pass-or-fail check, and while the request states what the requester wants to be true (steps they would actually do) it describes no observable behavior, user or scenario.
- Complexity 2: As written the only area implied is the backend agent module, `backend/src/agent/` and its prompt file `src/agent/prompts/breakdown.system.md`: one repo, a few files, no schema change.
- Risk 3: Any change aimed at suggestion quality lands on the breakdown prompt or the inputs handed to it.
- Architecture change: no
- Questions: 1. Who is doing this work, and can you point to one recent task where the suggested steps were not useful? 2. For that task, what would the assistant have suggested if it had got it right? 3. What about today's suggestions should stay exactly as it is?

## #4 Share a task with a teammate

- Clarity 5: Each criterion is checkable (share by email from the task detail, the task appearing in the other user's list with a "shared by <display name>" badge, either user's change visible to the other within five seconds, the error "No user with that email" on an unknown address, unsharing removing the task from the other user's list, and tags visible but not editable by the recipient), and it names three out-of-scope items plus the unknown-email edge case.
- Complexity 5: It restructures existing flows across both repos: a share relation in `backend/src/db/schema.ts` and `drizzle/`, and ownership checks in `src/services/` and `src/repositories/` moving from single-owner to owner-or-sharee on the existing task and step endpoints, with `frontend/src/features/tasks/` list and detail gaining the badge and cross-account polling.
- Risk 5: It changes a multi-user boundary directly, giving a second account read and write access to another user's task and its steps.
- Architecture change: yes
- Questions: none
